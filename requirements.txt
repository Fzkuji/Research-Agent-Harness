# Research Agent Harness — dependencies
#
# None. The harness only uses OpenProgram itself, which is deliberately
# NOT listed: it runs inside an existing OpenProgram install (see README
# — `openprogram programs install research` clones it in). Listing
# openprogram here would re-install the host from git on every install.

# Optional: PDF parsing (needed by load_paper / wiki ingest)
# pip install pymupdf      (or: pip install "research-agent-harness[pdf]")

# Optional: Wiki integration
# install via: openprogram programs install wiki
